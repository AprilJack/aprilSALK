---
title: "aprilSalk Package R 3.6.1"
output: html_notebook
editor_options: 
  chunk_output_type: console
---


First create your package using devtools.

```{r eval=FALSE}
#usethis::create_package("aprilSALK")
```

```{r}
#install.packages("devtools")
library(devtools)
#devtools::install_github("klutometis/roxygen")
library(roxygen2)
```

After adding functions navigate to ~/package/R directory and document() them to add them to the pacakge's namespace. 
```{r}
setwd("/gpfs/analyses/april/April/aprilSALK_R/R")
document()
```

Now navigate to the directory that contains your whole package directory, and install it. 

```{r}
setwd("/gpfs/analyses/april/April")
install("aprilSALK")
```
